# pbl_collector

Flutter PBL Collector

Remember to create a config file `assets/config/config.json`. One key is required to run the app: `pbl_service_address`.
The config loading is in `config/utils/config.dart`.
